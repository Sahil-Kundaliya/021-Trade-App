name: core_data
description: Shared storage and data infrastructure contracts.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
