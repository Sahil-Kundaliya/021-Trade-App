name: core_data
description: Shared storage and data infrastructure contracts.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0
  get_it: ^8.2.0
  injectable: ^2.5.2

dev_dependencies:
  build_runner: ^2.15.1
  flutter_lints: ^6.0.0
  injectable_generator: ^2.7.0

flutter:
  assets:
    - assets/mock/trading_mock_dataset.json
