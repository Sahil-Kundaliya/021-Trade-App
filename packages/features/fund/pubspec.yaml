name: fund
description: Fund feature presentation package.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.1

dependencies:
  core_data:
    path: ../../foundation/core_data
  core_ui:
    path: ../../foundation/core_ui
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get_it: ^8.2.0
  injectable: ^2.5.2
  navigation_contract:
    path: ../../navigation/navigation_contract
  url_launcher: ^6.3.2

dev_dependencies:
  bloc_test: ^10.0.0
  build_runner: ^2.15.1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.7.0

flutter:
